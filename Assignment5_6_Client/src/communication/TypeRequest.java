package communication;

import business.Customer;
//creating the enum class (list of constants with static access , as a part of class Request)
public enum TypeRequest {
	insert, update, find,exit;
}
